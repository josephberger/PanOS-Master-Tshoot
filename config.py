db_uri = 'sqlite:///mtdb.db'
timeout = 5
# Logging configuration
log_level = "INFO"  # You can set this to DEBUG, INFO, WARNING, ERROR, CRITICAL
log_file = "mt.log"
log_to_terminal = False # Set to True to also output logs to the terminal
